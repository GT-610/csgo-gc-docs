{
  "name": "csgo-gc-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "deploy": "bun run docs:build && wrangler deploy",
    "preview": "bun run docs:build && wrangler dev"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "wrangler": "^4.110.0"
  }
}
